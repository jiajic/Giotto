% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_expression.R
\name{markers_scran}
\alias{markers_scran}
\alias{analyzeData,matrix,scranMarkersParam-method}
\alias{analyzeData,Matrix,scranMarkersParam-method}
\alias{analyzeData,DelayedMatrix,scranMarkersParam-method}
\title{Pairwise Marker Detection (scran)}
\usage{
\S4method{analyzeData}{matrix,scranMarkersParam}(x, param, ..., groups = NULL)

\S4method{analyzeData}{Matrix,scranMarkersParam}(x, param, ..., groups = NULL)

\S4method{analyzeData}{DelayedMatrix,scranMarkersParam}(x, param, ..., groups = NULL)
}
\arguments{
\item{x}{expression values. A `matrix`, a `Matrix`, or a `DelayedMatrix`
(which is what `expression_values = "scaled"` holds).}

\item{param}{a [scranMarkersParam-class].}

\item{groups}{vector of group assignments, one per cell, in column order.}
}
\value{
marker detection results
}
\description{
Detect marker features by comparing each group against the others, as
implemented by \code{\link[scran]{findMarkers}}.

Every ordered pair of groups is tested, and the resulting p-values are
combined into a single ranked table per group. With the default
\code{test_type = "t"} the test is a Welch \eqn{t}-test on the per-group
moments:

\deqn{\LARGE
t = \frac{\bar{x}_{i,a} - \bar{x}_{i,b}}
         {\sqrt{s^2_{i,a}/n_a + s^2_{i,b}/n_b}}
}
Where:
\itemize{
\item (\eqn{\bar{x}_{i,a}}) is the mean of feature \eqn{i} over the cells of
group \eqn{a}
\item (\eqn{s^2_{i,a}}) is the variance of feature \eqn{i} over the same cells
\item (\eqn{n_a}) is the number of cells in group \eqn{a}
}

Because the statistic depends on the values only through
\eqn{n}, \eqn{\bar{x}} and \eqn{s^2}, the expression matrix is visited
once per analysis rather than once per comparison. That is what lets a
streaming backend implement the same test without materializing the matrix.

Which expression values are tested is the caller's choice — the methods use
whatever matrix they are given.
}
\section{params}{


\tabular{ll}{
\code{test_type} \tab character (default = "t"). Pairwise test: \code{"t"} Welch
\eqn{t}-test, \code{"wilcox"} rank-sum, \code{"binom"} binomial on detection
rates. Backends may support only a subset. \cr
\code{pval_type} \tab character (default = "any"). How the pairwise p-values
are combined per group: \code{"any"}, \code{"some"}, \code{"all"}. \cr
\code{comparison} \tab character (default = "pairwise"). \code{"pairwise"} tests
every ordered pair; \code{"one_vs_rest"} tests each group against the pooled
remainder. \cr
\code{direction} \tab character (default = "any"). \code{"any"}, \code{"up"}, \code{"down"}.
\cr
\code{lfc} \tab numeric (default = 0). Log-fold-change threshold to test
against. \cr
\code{std_lfc} \tab logical (default = FALSE). Report the effect size as a
standardized log-fold-change (Cohen's d). \cr
\code{min_prop} \tab numeric or NULL. Minimum proportion of comparisons a
feature must be significant in, for \code{pval_type = "some"}. \cr
\code{log_p} \tab logical (default = FALSE). Report p-values on the log
scale. \cr
\code{full_stats} \tab logical (default = FALSE). Retain the per-comparison
statistics as nested columns. \cr
\code{sorted} \tab logical (default = TRUE). Sort each group's table by
significance.
}

\code{pval_type} and \code{min_prop} describe how scran combines the pairwise
comparisons and have no meaning independent of it; see
\code{\link[scran]{findMarkers}}.
}

\seealso{
\link{analyze_param}, \code{\link[=markersParam]{markersParam()}}, \code{\link[=findScranMarkers]{findScranMarkers()}}
}
\concept{marker detection parameters}
