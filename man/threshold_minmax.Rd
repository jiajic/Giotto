% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{threshold_minmax}
\alias{threshold_minmax}
\title{Value MinMax Restriction/Clamping}
\value{
minmaxThreshParam
}
\description{
Set an \code{upper} and \code{lower} bound for the data. Values above \code{upper} will be
set to the \code{upper} value. Values below \code{lower} will be set to the \code{lower}
value.
}
\section{params}{


\tabular{ll}{
\code{upper} \tab numeric (default = Inf) highest acceptable value. Values
above this will be set to the same as \code{upper}. \cr
\code{lower} \tab numeric (default = -Inf) lowest acceptable value. Values
below this will be set to the same as \code{lower}.\cr
\code{values} \tab logical (\code{SpatRaster} only) If \code{FALSE} values outside the
clamping range become \code{NA}, if \code{TRUE}, they get the extreme values
}
}

\examples{
e <- GiottoData::loadSubObjectMini("exprObj")
# also works with matrix classes
max_e <- processData(e, thresholdParam("minmax", upper = 6))
force(max_e)

gimg <- GiottoData::loadSubObjectMini("giottoLargeImage")
# also works with SpatRasters
mm_img <- processData(gimg,
    thresholdParam("minmax", lower = 30, upper = 100)
)
plot(mm_img)
}
\seealso{
\link{process_param}

Other threshold parameters: 
\code{\link{threshold_binarize}}
}
\concept{threshold parameters}
