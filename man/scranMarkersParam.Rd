% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_expression.R
\name{scranMarkersParam}
\alias{scranMarkersParam}
\alias{analyzeData,matrix,scranMarkersParam-method}
\alias{analyzeData,Matrix,scranMarkersParam-method}
\alias{analyzeData,DelayedMatrix,scranMarkersParam-method}
\title{Construct a \link[=scranMarkersParam-class]{scranMarkersParam}}
\usage{
scranMarkersParam(
  test_type = c("t", "wilcox", "binom"),
  pval_type = c("any", "some", "all"),
  comparison = c("pairwise", "one_vs_rest"),
  direction = c("any", "up", "down"),
  lfc = 0,
  std_lfc = FALSE,
  min_prop = NULL,
  log_p = FALSE,
  full_stats = FALSE,
  sorted = TRUE,
  ...
)

\S4method{analyzeData}{matrix,scranMarkersParam}(x, param, ..., groups = NULL)

\S4method{analyzeData}{Matrix,scranMarkersParam}(x, param, ..., groups = NULL)

\S4method{analyzeData}{DelayedMatrix,scranMarkersParam}(x, param, ..., groups = NULL)
}
\arguments{
\item{test_type}{character. Pairwise test to apply. \code{"t"} (default) is a
Welch \eqn{t}-test, \code{"wilcox"} a rank-sum test, \code{"binom"} a binomial test
of detection rates. Backends may support only a subset.}

\item{pval_type}{character. How the pairwise p-values are combined into one
per group: \code{"any"}, \code{"some"}, or \code{"all"}.}

\item{comparison}{character. \code{"pairwise"} (default) tests every ordered pair
of groups and combines them per group, as
\code{\link[scran]{findMarkers}} does. \code{"one_vs_rest"} instead tests each
group against the pooled remainder, returning one two-group table per
group — the shape \code{\link[=findScranMarkers_one_vs_all]{findScranMarkers_one_vs_all()}} consumes.}

\item{direction}{character. \code{"any"}, \code{"up"}, or \code{"down"}.}

\item{lfc}{numeric (default = 0). Log-fold-change threshold to test
against.}

\item{std_lfc}{logical (default = \code{FALSE}). Report the effect size as a
standardized log-fold-change (Cohen's d) rather than a raw one.}

\item{min_prop}{numeric or \code{NULL}. Minimum proportion of comparisons a
feature must be significant in, for \code{pval_type = "some"}.}

\item{log_p}{logical (default = \code{FALSE}). Report p-values on the log scale.}

\item{full_stats}{logical (default = \code{FALSE}). Retain the per-comparison
statistics as nested columns.}

\item{sorted}{logical (default = \code{TRUE}). Sort each group's table by
significance.}

\item{...}{additional named entries to attach to \verb{@param}.}

\item{x}{expression values. A `matrix`, a `Matrix`, or a `DelayedMatrix`
(which is what `expression_values = "scaled"` holds).}

\item{param}{a [scranMarkersParam-class].}

\item{groups}{vector of group assignments, one per cell, in column order.}
}
\value{
A \link[=scranMarkersParam-class]{scranMarkersParam} object.
}
\description{
Factory for the pairwise marker detection analysis parameter.
Parameter names follow scran's \code{findMarkers} in snake_case; see
\code{\link[scran]{findMarkers}} for the full statistical description.
}
\examples{
p <- scranMarkersParam(test_type = "t", pval_type = "any")
}
